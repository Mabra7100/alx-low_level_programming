#include <stdio.h>

/** 
 *
 * Main - entry point
 * Return: return 0
 *
 */

int main(void)
{
  char word[] = "_putchar";
  for(int c = 0, a < 8; c++)
  {
    _putchar(word[c]);
    
  }
  _putchar('\n');
  return(0);
  
}
